[project]
name = "yanmo-tools"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "LeonHerben", email = "herberten16@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "onnxruntime>=1.30.0",
    "sherpa-onnx>=1.13.8",
    "sounddevice>=0.5.6",
]

[project.scripts]
yanmo-tools = "yanmo_tools:main"

[build-system]
requires = ["uv_build>=0.12.19,<0.13.0"]
build-backend = "uv_build"
